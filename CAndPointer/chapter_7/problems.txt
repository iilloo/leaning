P434
1.可以在存根内部打印存根已经被调用的信息
2.缺点
3.如果能进行隐式类型转换，则没有问题，如果不能编译器会报错
4.编译器提出警告，返回值被抛弃
5.如果能进行隐式类型转换，则没有问题，如果不能编译器会报错

6.编译器假设该函数的返回值类型为整型，
    没有原型的函数，现代c标准已经抛弃

7.传入的数组必须是10个整型元素的，缺乏普适性
8.都是循环，都有循环的出口，每次循环之后都向着循环出口更近一步
9.避免了繁琐的手动拷贝
10.懒得想